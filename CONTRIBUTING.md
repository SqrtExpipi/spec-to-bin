# Contributing

## Development

```bash
npm ci
npm run dev
```

Before opening a pull request:

```bash
npm run test:run
npm run build
```

Keep public examples fictional. Do not commit customer specifications, credentials, internal protocol details, or production binary data.

Changes to binary generation or validation should include focused test vectors with exact expected bytes.
